[
{
  "model": "siem.parsehelper",
  "fields": {
    "name": "pam_aggr",
    "desc": "PAM aggregated event count",
    "match_regex": "PAM (\\d+) more authentication failure",
    "fields": "aggregated_events"
  }
},
{
  "model": "siem.parsehelper",
  "fields": {
    "name": "pam_auth_shost",
    "desc": "PAM authentication source host field.",
    "match_regex": ".*rhost=(\\S*)\\s.*",
    "fields": "source_host"
  }
},
{
  "model": "siem.parsehelper",
  "fields": {
    "name": "pam_auth_user",
    "desc": "PAM authentication user field.",
    "match_regex": "\\suser=(\\S*)",
    "fields": "username"
  }
}
]
